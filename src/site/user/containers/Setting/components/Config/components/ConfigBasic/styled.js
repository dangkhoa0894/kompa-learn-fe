import styled from 'styled-components';

export const ConfigBasicContain = styled.div`
  input {
    border-radius: 5px;
  }
  .ant-form-item {
    margin-bottom: 0px;
  }
  .input-descriptions {
    resize: none;
  }
  .group-button {
    justify-content: flex-end;
  }
`;
