import styled from 'styled-components';

export const ContainGaugeChart = styled.div`
  span {
    width: 100%;
    display: flex;
    justify-content: center;
    transform: translateY(-15px);
    font-size: 0.9rem;
    text-transform: capitalize;
  }
`;
