import styled from 'styled-components';

export const ContainerRename = styled.div`
  width: 400px;
  .rename-input {
    border-radius: 10px;
    margin: 20px 0px;
  }
  .group-btn {
    display: flex;
    justify-content: flex-end;
    .btn-rename {
      border-radius: 3px;
      margin-left: 5px;
    }
    div {
      width: fit-content;
    }
  }
`;
